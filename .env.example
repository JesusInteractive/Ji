# Copy to .env (gitignored) and fill in real values.

# Base URL for the backend api.ts and services/tts.ts call. EXPO_PUBLIC_
# vars are inlined into the client bundle at build time by Expo/Metro --
# don't put secrets here, only public config like this.
EXPO_PUBLIC_API_BASE=https://api.jesusinteractive.com
