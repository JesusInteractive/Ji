{
  "name": "jesus-interactive-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal reference backend for src/services/api.ts's sendMessage() -- holds the Anthropic API key server-side, injects the Jesus persona, never ships to the client.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "@elevenlabs/elevenlabs-js": "^2.64.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "multer": "^2.0.1"
  }
}
